public class Multadd {
    public static double multadd(double a, double b, double c) {
        //continuar segunda parte aqui
        return a * b + c;
    }

    public static double expSum(double x){
        
        multadd(a, b, c);
    }

    public static void main(String[] args) {

        double pi_four= Math.PI / 4;
        double pi_cos = pi_four / 2;
        double result = Math.sin(pi_four) + Math.cos(pi_cos);
        double first_log = Math.log(10);
        double second_log = Math.log(20);
        
        System.out.println(multadd(1.0, 2.0, 3.0));
        System.out.println(multadd(result, first_log, second_log));
    }
}
